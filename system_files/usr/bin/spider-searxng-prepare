#!/bin/bash
set -euo pipefail

config_dir="${XDG_CONFIG_HOME:-$HOME/.config}/spider-os/searxng"
install -d -m 0700 "$config_dir"
if [ ! -f "$config_dir/settings.yml" ]; then
  install -m 0600 /usr/share/spider-os/searxng/settings.yml "$config_dir/settings.yml"
fi

